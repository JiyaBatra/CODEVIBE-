# Optional: override API base URL when running the client against a local backend.
# Many components still call the production Render URL; set this when you add
# VITE_API_BASE_URL support across the client.
VITE_API_BASE_URL=http://localhost:5002
